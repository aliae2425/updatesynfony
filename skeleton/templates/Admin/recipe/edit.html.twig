{% extends 'Admin/admin.html.twig' %}


{% block title %}
    {% if recette is defined %}
        Modifier {{recette.titre}}
    {% else %}
        Ajouter une recette
    {% endif %}
{% endblock %}

{% block body %}

    {% if recette is defined %}
        <h1>Modifier {{recette.titre}}</h1>
    {% else %}
        <h1>Ajouter une recette</h1>
    {% endif %}

    <img src="{{ vich_uploader_asset(recette, "thumbnailFile") }}" alt="xxx">

    {{form(form)}}


{% endblock %}